# Parameters are the variables listed inside the parentheses in the function definition.
# Arguments are the values passed into the function when it is called.

def iFunction(x):
    print("Hello, ", x)
    return x

y = iFunction("Universe!")

print(y)

