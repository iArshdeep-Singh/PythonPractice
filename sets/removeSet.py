# remove(): Removes a specified element from the set.
# Raises a KeyError if the element is not present.

iSet = {"add()", "remove()", "clear()", "insert()", "append()", "extend()", "sort()", "pop()", "sort()", "reverse()", "copy()"}

print(iSet)

iSet.remove("copy()")
iSet.remove("reverse()")
iSet.remove("pop()")

print(iSet)